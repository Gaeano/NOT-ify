<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SpotifyClone" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/blackBg</item>
        <item name="android:colorBackground">@color/blackBg</item>

        <item name="android:statusBarColor">@color/blackBg</item>

        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.SpotifyClone" parent="Base.Theme.SpotifyClone" />
</resources>